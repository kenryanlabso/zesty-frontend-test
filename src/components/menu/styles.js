import { Link } from 'react-router-dom';
import styled from 'styled-components';

const MenuStyles = {
  Link: styled(Link)`
    font-size: 18px;
    padding: 12px;
  `,
};

export default MenuStyles;
